#include "main.h"
#include <stdio.h>

/**
 * mul - function that multipies two intergers
 * @a: int to be mul
 * @b: int to be mul
 * Return: Always 0.
 */

int mul(int a, int b)
{
	return (a * b);
}
